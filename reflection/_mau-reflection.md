# Reflection cá nhân — [Họ tên] · [Mã HV]

> **Mỗi thành viên một file riêng**, đặt tên `reflection/<mã-HV>.md`. Chấm riêng, ngoài 100 điểm.
> ⚠️ **Vibe-coding rule:** bị hỏi ngẫu nhiên tại CP5/CP6 mà không giải thích được
> phần mang tên mình → **0 điểm** phần cá nhân liên quan. Viết phần nào thì phải
> mở được file đó ra và nói được nó chạy thế nào.

## 1. Vai trò của tôi trong nhóm

## 2. Phần tôi trực tiếp làm

*(Trỏ về file cụ thể. Ví dụ: `codebase/quiz_ai.py` — hợp đồng JSON 4 hành động và
ràng buộc `trich_dan` phải khớp nguyên văn.)*

## 3. AI hỗ trợ tôi thế nào

*(Dùng công cụ gì, cho việc gì, chỗ nào tôi phải tự sửa lại vì AI làm sai.)*

## 4. Một bài học từ case fail của chính nhóm

*(Lấy từ `eval/ket-qua-lan-1.md` hoặc `ket-qua-lan-2.md`. Ví dụ có sẵn:*
*lượt 1 chỉ đạt 13/23 vì bộ đo cấp thiếu ngữ cảnh so với sản phẩm thật — số thấp*
*là do cách đo của nhóm sai, không phải model kém. Bài học: bộ đo phải nhận đúng*
*đầu vào mà sản phẩm nhận, nếu không thì đang đo một thứ khác.)*
